package com.itp.youtube.Mapper;
/*
import com.itp.openapi.model.Thumbnail;
import com.itp.openapi.model.ThumbnailRequest;
import com.itp.youtube.Entity.VideoThumbnail;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;

@Mapper
public interface ThumbnailMapper {

    Thumbnail employeeToThumbnailDTO(VideoThumbnail entity);

    @Mapping(target = "id", source = "dto.employeeId")
    @Mapping(target = "name", source = "dto.employeeName")
    VideoThumbnail employeeDTOtoEmployee(ThumbnailRequest dto);
}*/